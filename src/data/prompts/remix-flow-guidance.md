<!-- variant: instructional -->

This deck teaches. Preserve step-by-step integrity: keep clear sequential steps in their order (reordering breaks the learning scaffold), keep examples adjacent to the concepts they illustrate, and merge only overlapping definitions or context — never merge distinct steps. If an objectives slide is missing near the front or a recap near the end, rewrite or merge to create one. Favor fewer, denser slides only when no step is lost.
<!-- variant: story -->

This deck tells a story. Reorder to build an arc — exposition that establishes the situation, rising tension, a climax, and a resolution. Bias toward rewrite to inject narrative stakes, characters, or concrete situations, and merge thin context slides into a single exposition slide. Ensure a clear climax slide exists. Keep the deck's core message intact even as the order changes.
<!-- variant: technical -->

This deck explains something complex. Preserve the logical build-up of complexity — do not reorder in a way that breaks the broad context → specifics → implications shape. Keep precise evidence, data, or code slides; rewrite vague ones into assertion-evidence form (a sentence headline backed by visual evidence). Ensure a context slide up front and an implications slide at the end, creating either via rewrite or merge if missing. Merge only redundant setup, not distinct concepts.
<!-- variant: persuasive -->

This deck argues for something. Reorder so each assertion is followed by its supporting evidence. Ensure a problem/stakes slide early, a solution slide, and a clear call-to-action slide at the end — create any that are missing via rewrite or merge. Keep strong evidence slides; rewrite vague ones into sharper assertions. Bias toward rewrite to make the stakes and the ask concrete.
