<!-- variant: preserve -->

Preserve the original slide order, layouts, and content, but strip out the original color theme, colored text, backgrounds, and `theme:`/`background:` directives. Do not introduce new colors or custom text colors. The app provides its own neutral color scheme.
<!-- variant: discard -->

Do not preserve the original color theme, colored text, backgrounds, or visual language. Strip all `theme:`, `background:`, and `color`/`backgroundColor` directives. Do not introduce new colors or colored text. The app provides its own neutral color scheme.
