<!-- variant: preserve -->

Preserve the original color theme, colored text, backgrounds, and `theme:`/`background:` directives. The restructured deck keeps the source slides' visual identity — do not ask the execute phase to strip, change, or replace the theme, backgrounds, or colors. Do not introduce new `theme:`, `background:`, `color`, or `backgroundColor` directives.
<!-- variant: discard -->

Do not preserve the original color theme, colored text, backgrounds, or visual language. Strip all `theme:`, `background:`, and `color`/`backgroundColor` directives. Do not introduce new colors, `theme:`, `background:`, or colored text. The app provides its own neutral color scheme.
