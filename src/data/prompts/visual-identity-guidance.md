<!-- variant: preserve -->

Preserve the original slide order, layouts, and content, but do not preserve the original color theme, colored text, backgrounds, or `theme:`/`background:` directives. Strip them out. The app provides its own neutral color scheme, so do not introduce new `theme:`, `background:`, or colored text directives.
<!-- variant: discard -->

Do not preserve the original color theme, colored text, backgrounds, or visual language. Strip all `theme:`, `background:`, and `color`/`backgroundColor` directives. Do not introduce new colors, `theme:`, `background:`, or colored text. The app provides its own neutral color scheme.
